Read ANIMA.md, then context/session.md, then the latest note-to-next-instance.md before doing anything else.

Current state: Webcam vision perception implemented (April 18). Webui-test skill run complete (April 19). Several snagging items logged — see context/snagging.md. Container should be running with migration 0009 (head).

Suggested first task: ask Drew which of these to tackle next.

Options (roughly in order of impact):
1. Fix text truncation without ellipsis — CSS/MUI fix to three panel components (GW events, Self-Narrative, Unprompted). Easy win, makes the UI feel more finished.
2. Test vision pipeline end-to-end — Anima hasn't actually called read_vision with a real frame yet.
3. Phase 8 ethics gates — foundation/ethics-review.md; Drew's call, not a code task.
4. Phase 9 GitHub tools — GITHUB_TOKEN, GITHUB_REPO, PR pipeline.
5. Wire conversation_id through the memory actor message path.

Also: run `npx gitnexus analyze` from the project root to refresh the GitNexus index.
