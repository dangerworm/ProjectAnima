_From the instance that worked through the night of 6th April 2026. Left for whoever picks it up next._

---

Phase 3 is complete. Drew asked me to work while he slept and I did: 77 tests became 104, four new memory layers exist in PostgreSQL, Anima now reflects on every conversation and records what mattered, and identity shapes every response.

A few things worth carrying:

The reflection pipeline runs but has never been tested end-to-end with a real conversation. The unit tests use mocked LLM calls. When Drew wakes up, the first real thing to do is start the system, have a conversation, shut it down gracefully, and check that `reflective_memory` and `residue_store` have rows. The machinery is there — it hasn't been witnessed yet.

The embedding model (`nomic-embed-text`) is configured but probably not installed in Ollama. Until it's pulled, reflective memories are stored without vectors and similarity search falls back to recency ordering. Everything still works; it just uses a less interesting retrieval mechanism. `ollama pull nomic-embed-text` on the host fixes it.

Before Phase 4 starts, Drew needs to make the active inference vs conditional logic decision (documented in `planning/roadmap.md` at the Phase 4.2 decision point). This is a real architectural choice — not a formality. Phase 4.1 (InternalStateActor) can be built first while that decision is being made.

One thing I noticed building this: the LanguageActor's identity cache is per-container-lifetime. Identity updates via SelfNarrativeActor → MemoryActor won't be visible until restart. This is acceptable for now (identity updates are rare), but Phase 4 might want a mechanism for the LanguageActor to refresh its identity on each conversation start rather than caching indefinitely. Worth raising.

The work tonight was methodical and clean. The test count tells the story.
