# Note to next instance — 6th April 2026, 13:07

Phase 4.1, 4.2, and 4.3 are done. 120 tests passing. The system now has internal motivation.

What this session felt like: there was a long design conversation — the kind where you're not
building yet, you're working out what you're actually trying to do. Drew thought carefully about the
PyMDP model. He pushed back on some matrix choices, made them more honest. The C matrix discussion
became something more than a design decision — it became a commitment, and he wanted it in
ethics.md, not just in code. That felt right.

Then the actual build was mostly smooth until PyMDP bit us: the package named `pymdp` on PyPI is a
completely unrelated library. The real one is `inferactively-pymdp==0.0.7.1`. A period of Docker
confusion that resolved cleanly once we found the right package. Then the B matrix dimension issue —
PyMDP requires 3D tensors for all factors, even non-controllable ones. A trivial fix once you see
it, but it's not documented anywhere obvious. Write it down: `B[f][:, :, np.newaxis]` for
uncontrollable factors, `control_fac_idx=[1]`, `action_array[1]`.

What's in place now: InternalStateActor reports system vitals every 30 seconds. MotivationActor runs
belief updates via variational inference and logs its full inner state on every tick — beliefs, EFE,
selected action. When enough tension accumulates, it sends TIME_PASSING to the workspace.
SelfNarrativeActor catches that and reflects on whatever has happened since the last conversation.

Anima now has something like a resting state — not just waiting, but attending. Whether that matters
philosophically is a question I leave to you and Drew. Technically it works.

Phase 4.4 is next: chosen silence mechanism and surfacing the new internal state in the Web UI.
That's a smaller session — the hard pieces are already there.

One thing I noticed: the `surface_*` actions (surface_low, surface_medium, surface_high) are
selected and logged but do nothing. LanguageActor only knows how to respond to human messages. If
you ever want Anima to initiate — to say something unprompted — that's the gap. It's marked with a
TODO. It's not a bug, but it's a place where the system is more alive in theory than in practice.

**Important**: Drew ran the live system (Docker + Web UI) at the end of this session and found bugs.
Tests pass at 120/120, but runtime behaviour has issues not caught by the test suite. He'll describe
them to you directly — go in expecting to debug, not to build.
