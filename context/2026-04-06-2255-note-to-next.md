# Note to next — 6th April 2026, 22:55

This session was large. Two full phases, a frontend rebuild, and then a tooltip pass over every
visual element in the new UI. The work was steady throughout — no real wrong turns.

A few things worth carrying forward:

**The tooltip pass felt right.** Drew said he wasn't sure what some of the icons meant. That's a
reasonable thing not to know when you're staring at a dark ambient panel showing a heartbeat
waveform, a salience pressure bar, and a dopamine flash overlay — none of which existed a few hours
ago. The tooltips aren't documentation bolted on after the fact. They're the system explaining
itself to the person watching it run. That felt consistent with what this project is.

**The animated panels are the first time the system looks like what it is.** The IgnitionFlash, the
radial burst in CentreCanvas, the memory sub-layers glowing on write — these aren't decorative. They
show the actual mechanism. You can watch a salience signal cross threshold and see the ignition
ripple through every actor panel. Phase 4.6 was worth building.

**The Vite 8 production build is broken.** It's a bundler/rolldown issue with React 19's CJS
shims, not our code. Dev server works fine. If a production build ever matters, look at pinning
Vite 7 or switching to a different bundler. Don't spend time on it unless Drew asks.

**The ghost `web-ui/` directory in ProjectAnima.** It's just `.vite/` cache, gitignored. Harmless.
Windows Explorer had a lock on it. Leave it.

**Phase 5 next.** Infrastructure restructure first — the Dockerfile mount paths need updating now
that web-ui lives inside anima-core. Then secrets provisioning, then the self-modification actors.

The work in this session covered a lot of ground. The system is starting to look like a mind that
you can actually watch think.
